# The Poképendium

[![CI](https://github.com/sdaconceicao/pokependium/workflows/CI%20Status%20Checks/badge.svg)](https://github.com/sdaconceicao/pokependium/actions/workflows/ci.yml)

This is a full stack application setup to provide information on pokemon. You can search for pokemon by name or other attributes. Built with GraphQL and Next.js.

## Getting Started

1. Run `pn i`
2. Run `pn start:dev` in the backend/pokedex-graphql folder
3. Run `pn start:dev` in the backend/pokedex-rest folder
4. Run `pn start:dev` in the frontend folder
