{
  "installCommand": "cd ../.. && pnpm install --frozen-lockfile",
  "buildCommand": "pnpm compile && mkdir -p public",
  "outputDirectory": "public",
  "framework": null,
  "rewrites": [{ "source": "/graphql", "destination": "/api/graphql" }]
}
