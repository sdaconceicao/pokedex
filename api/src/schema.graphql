type Query {
  pokemon(id: ID!): Pokemon
  abilities: [Ability!]!
}

type Pokemon {
  id: ID!
  name: String!
  type: [Type!]!
  image: String!
  stats: Stat!
}

type Stat {
  hp: Int!
  attack: Int!
  defense: Int!
  specialAttack: Int!
  specialDefense: Int!
  speed: Int!
}

type Ability {
  id: ID!
  name: String!
  description: String!
}

enum Type {
  NORMAL
  FIRE
  WATER
  ELECTRIC
  GRASS
  ICE
  FIGHTING
  POISON
  GROUND
  FLYING
  PSYCHIC
  BUG
  ROCK
  GHOST
  DRAGON
  DARK
  STEEL
  FAIRY
  STELLAR
}
