/**
 * The children slot has no page for /region/[name]/pokemon/[id], so landing on
 * that URL cold renders the region view here — the list sits behind the detail
 * exactly as it does when you open a Pokemon from it.
 */
export { default } from "./page";
