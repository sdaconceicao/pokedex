/**
 * The children slot has no page for /type/[name]/pokemon/[id], so landing on
 * that URL cold renders the type view here — the list sits behind the detail
 * exactly as it does when you open a Pokemon from it.
 */
export { default } from "./page";
